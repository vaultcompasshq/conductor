name: Conductor and Vault Guard

# Post-publish sample: this workflow assumes
# @vaultcompass/conductor-cli@0.3.0-beta.1 is available on npm.
# vault-guard is installed from its published npm package.

on:
  pull_request:

jobs:
  guardrails:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install Conductor
        run: pnpm dlx @vaultcompass/conductor-cli@0.3.0-beta.1 --version

      - name: Collect changed paths
        id: changed
        shell: bash
        run: |
          git fetch origin "${{ github.base_ref }}" --depth=1
          paths="$(git diff --name-only "origin/${{ github.base_ref }}...HEAD" | paste -sd, -)"
          echo "paths=${paths}" >> "$GITHUB_OUTPUT"

      - name: Check intent drift
        run: |
          pnpm dlx @vaultcompass/conductor-cli@0.3.0-beta.1 drift \
            --ci \
            --contract .conductor/intent-contract.yaml \
            --paths "${{ steps.changed.outputs.paths }}"

      - name: Check secrets
        run: npx --yes @vaultcompass/vault-guard@latest -- scan . --format text
