{
  "name": "conductor",
  "private": true,
  "version": "0.0.0",
  "description": "Intent fidelity for AI-assisted development",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
