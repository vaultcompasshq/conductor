contract_id: ic-20260603-pvt001
version: "1.0.0"
original_ask: "Add real-time CSV export via API instead of client-side download."
in_scope:
  - "CSV export endpoint returning current fund table data"
  - "Export button triggers download from API response"
out_of_scope:
  - "WebSocket streaming export"
  - "PDF or email export formats"
constraints:
  - source: user-stated
    rule: "Document any scope change in the pivot log before proceeding"
    priority: critical
acceptance_criteria:
  - id: ac-1
    description: "Export button downloads CSV generated by the API endpoint"
    testable: true
frozen_at: "2026-06-03T09:00:00Z"
frozen_by: user
pivot_log:
  - timestamp: "2026-06-03T11:00:00Z"
    change: "Pivot from client-side export to server-generated CSV"
    reason: "Desk workflow requires live data at export time"
    acknowledged_by: user
    updates:
      in_scope_added:
        - "Server-side CSV generation endpoint"
      out_of_scope_added:
        - "Client-side-only export without API"
